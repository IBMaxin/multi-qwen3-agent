# Ollama Configuration
MODEL_SERVER=http://localhost:11434/v1
MODEL_NAME=qwen3:4b-instruct-2507-q4_K_M
# Other available models:
# qwen3:4b
# qwen3:4b-thinking-2507-q4_K_M
# qwen3:4b-instruct-2507-q4_K_M
# qwen3-vl:latest
# phi4-mini:3.8b
API_KEY=EMPTY

# Embeddings for Local RAG (LocalVectorSearch tool)
EMBEDDING_MODEL=qwen3-embedding:4b
# Other embedding options:
# qwen3-embedding:0.6b  (faster, smaller)
# qwen3-embedding:8b    (best quality)
EMBEDDING_BASE_URL=http://localhost:11434

# Vector Store Persistence (for web ingestion pipeline)
VECTOR_STORE_PATH=./workspace/vector_stores
# Where FAISS indexes are saved/loaded from

# Optional: Generation parameters (override defaults)
# GEN_TEMPERATURE=0.7
# GEN_TOP_P=0.8
# GEN_MAX_INPUT_TOKENS=6000
# FN_CALL_PROMPT_TYPE=nous

# Monitoring (optional)
SENTRY_DSN=
